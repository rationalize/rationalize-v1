import React from "react";
import { HelpPopover } from "../../HelpPopover";

export function CriteriaHelp() {
  return (
    <HelpPopover>
      Criteria answers the question of “What do you need to consider when making
      your evaluation?”. Basically, it is a set of dimensions that will be used
      to prioritize the Concepts.
    </HelpPopover>
  );
}
