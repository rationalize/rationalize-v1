import React from "react";

import { HelpPopover } from "components/HelpPopover";

export function CriteriaHelp() {
  return (
    <HelpPopover>
      These are the attributes or characteristics that you deem to be important
      when choosing between your options. Attributes answer the question of
      “What do you need to consider when making your decision?” or “What matters
      when making this decision?”
    </HelpPopover>
  );
}
